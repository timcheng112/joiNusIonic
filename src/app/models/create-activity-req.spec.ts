import { CreateActivityReq } from './create-activity-req';

describe('CreateActivityReq', () => {
  it('should create an instance', () => {
    expect(new CreateActivityReq()).toBeTruthy();
  });
});
